# 对象序列化
* 将对象写入对象，会失去对象的属性信息  
/eg：编写函数，记录student类的对象信息，用一行写入文件

使用 `ofstream(filename,ios::app)`

* 文件位置指针
istream : `seekg(n,ios::beg)//开始,n 的单位是字节`  
    n代表位移，-代表向前;  
    第二个参数：beg (beggin) ; cur (current) ; end ;  
    `obj.eof()`:判断光标是否在文件尾部.  
    `obj.clear()`:

# 更新顺序文件
## 解决方案
使用二进制文件来记录，**前提：文件大小可计算**


